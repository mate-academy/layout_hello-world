import Config from "./config";
import Issue from "./issue";
import { LinterConfig } from "./read-config";
import { Document } from "@linthtml/dom-utils/lib/dom_elements";
export default class Linter {
    parse_fn: (html: string) => Document;
    config: Config;
    constructor(config: LinterConfig);
    /**
     * Lints the HTML with the options supplied in the environments setup.
     */
    lint(html: string): Promise<Issue[]>;
    private lint_DOM;
    private call_rule_lint;
    private reset_rules;
}
//# sourceMappingURL=linter.d.ts.map